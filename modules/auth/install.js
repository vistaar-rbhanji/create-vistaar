/**
 * Runtime installer for the auth module.
 * Typed twin: see install.ts (documentation / editor support).
 */

export async function install(context) {
  await context.helpers.standardInstall(context);
}
