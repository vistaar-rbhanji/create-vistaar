/**
 * Shared command utilities barrel.
 * Keep creation vs management helpers here so neither CLI entry owns them.
 */

export {
  FUTURE_ADD_MODULES,
  printComingSoon,
  type FutureAddModule,
} from "./coming-soon.js";
