/**
 * Login page stub for {{PROJECT_NAME}}.
 */
export default function LoginPage() {
  return (
    <main>
      <h1>Sign in to {{PROJECT_NAME}}</h1>
      <p>Authentication UI scaffold — implement form handling next.</p>
    </main>
  );
}
