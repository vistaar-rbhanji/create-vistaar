{
  "scripts": {
    "postinstall": "prisma generate",
    "db:migrate": "prisma db push && node src/mark-migrated.js",
    "db:seed": "node src/seed.js",
    "db:setup": "npm run db:migrate && npm run db:seed"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
